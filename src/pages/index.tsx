import { Heading } from '@aws-amplify/ui-react';
import * as React from 'react'; 

const HomePage = () => {
    return (
        <Heading level={2}>
            CDK and Amplify
        </Heading>
    );
}

export default HomePage; 